# Telegram Mini App - Click Game

Простое Telegram Mini App приложение в стиле Notcoin с лидербордом и базой данных.

## 🚀 Особенности

- 🎮 Простая игра с кликами (как Notcoin)
- 🏆 Лидерборд с топ-10 игроков
- 💾 Сохранение данных в PostgreSQL
- 📱 Полная интеграция с Telegram Web App
- 🔄 Данные сохраняются при перезагрузке, входе и выходе

## 📁 Структура проекта

```
.
├── backend/          # Express сервер с API
├── frontend/         # React приложение для Telegram
├── netlify.toml      # Конфигурация для Netlify (frontend)
└── render.yaml       # Конфигурация для Render (backend)
```

## 🛠️ Установка и запуск локально

### Backend

1. Перейдите в папку backend:
```bash
cd backend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env`:
```env
PORT=3000
DATABASE_URL=postgresql://user:password@localhost:5432/telegram_miniapp
NODE_ENV=development
```

4. Запустите сервер:
```bash
npm start
# или для разработки с автоперезагрузкой:
npm run dev
```

### Frontend

1. Перейдите в папку frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env`:
```env
VITE_API_URL=http://localhost:3000
```

4. Запустите dev сервер:
```bash
npm run dev
```

## 🌐 Деплой

> 📚 **ВАЖНО**: Если вы новичок в деплое, откройте файл **[DEPLOY_INSTRUCTIONS.md](./DEPLOY_INSTRUCTIONS.md)** - там подробная пошаговая инструкция с объяснением каждого шага!

### Краткая инструкция

**Backend на Render:**
1. Создайте PostgreSQL базу данных на Render
2. Создайте Web Service, подключите GitHub репозиторий
3. Build Command: `cd backend && npm install`
4. Start Command: `cd backend && node server.js`
5. Добавьте переменные: `NODE_ENV=production`, `DATABASE_URL`, `PORT=3000`

**Frontend на Netlify:**
1. Подключите GitHub репозиторий
2. Build command: `cd frontend && npm install && npm run build`
3. Publish directory: `frontend/dist`
4. Добавьте переменную: `VITE_API_URL` (URL вашего backend)

**Telegram Bot:**
1. Откройте @BotFather в Telegram
2. Используйте `/setmenubutton` для настройки кнопки меню
3. Укажите URL вашего Netlify сайта

**📖 Для подробной инструкции с скриншотами и объяснениями смотрите [DEPLOY_INSTRUCTIONS.md](./DEPLOY_INSTRUCTIONS.md)**

## 📝 API Endpoints

### POST `/api/user`
Создает или обновляет пользователя
```json
{
  "id": 123456789,
  "username": "username",
  "first_name": "Name"
}
```

### POST `/api/click`
Добавляет монеты за клик
```json
{
  "telegram_id": 123456789,
  "coins": 1
}
```

### GET `/api/leaderboard?limit=10`
Получает топ игроков

### GET `/api/user/:telegram_id`
Получает данные пользователя

## 🔧 Технологии

- **Backend**: Node.js, Express, PostgreSQL
- **Frontend**: React, Vite
- **Deployment**: Netlify (frontend), Render (backend)

## 📱 Использование

1. Откройте бота в Telegram
2. Нажмите на кнопку меню или отправьте `/start`
3. Кликайте по монете, чтобы зарабатывать монеты
4. Смотрите свой рейтинг в лидерборде

## ⚠️ Важные замечания

- Убедитесь, что ваш backend URL добавлен в CORS настройках
- Для продакшена используйте HTTPS
- Не коммитьте `.env` файлы с реальными токенами
- Токен бота уже указан в `backend/bot.js` - используйте его для настройки

## 🐛 Troubleshooting

- Если данные не сохраняются, проверьте подключение к базе данных
- Если приложение не открывается в Telegram, проверьте URL в BotFather
- Для локальной разработки используйте ngrok или подобные сервисы для тестирования Telegram Web App

