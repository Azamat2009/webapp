# 📚 ПОЛНАЯ ПОШАГОВАЯ ИНСТРУКЦИЯ ПО ДЕПЛОЮ

Эта инструкция проведет вас через весь процесс деплоя от начала до конца.

---

## 📋 ЧТО НУЖНО ПЕРЕД НАЧАЛОМ

1. ✅ Аккаунт на GitHub (если нет - создайте на github.com)
2. ✅ Аккаунт на Render.com (бесплатный)
3. ✅ Аккаунт на Netlify.com (бесплатный)
4. ✅ Ваш проект должен быть загружен на GitHub

---

## 🗄️ ЧАСТЬ 1: ДЕПЛОЙ BACKEND НА RENDER

### Шаг 1.1: Регистрация на Render

1. Откройте сайт: https://render.com
2. Нажмите кнопку **"Get Started for Free"** или **"Sign Up"**
3. Зарегистрируйтесь через GitHub (рекомендуется) или email
4. Подтвердите email, если потребуется

### Шаг 1.2: Создание PostgreSQL базы данных

1. В панели Render нажмите кнопку **"New +"** (вверху справа)
2. Выберите **"PostgreSQL"**
3. Заполните форму:
   - **Name**: `telegram-miniapp-db` (или любое другое имя)
   - **Database**: оставьте по умолчанию
   - **User**: оставьте по умолчанию
   - **Region**: выберите ближайший (например, Frankfurt)
   - **PostgreSQL Version**: оставьте последнюю версию
   - **Plan**: выберите **Free** (бесплатный план)
4. Нажмите **"Create Database"**
5. ⏳ Подождите 1-2 минуты, пока база создается

### Шаг 1.3: Получение DATABASE_URL

1. После создания базы данных, нажмите на неё в списке
2. Найдите раздел **"Connections"**
3. Найдите поле **"Internal Database URL"** или **"Connection Pooling"**
4. Скопируйте **Internal Database URL** (выглядит примерно так: `postgresql://user:password@dpg-xxxxx-a.oregon-postgres.render.com/dbname`)
5. ⚠️ **ВАЖНО**: Сохраните этот URL в блокнот или текстовый файл - он понадобится позже!

### Шаг 1.4: Загрузка проекта на GitHub (если еще не загружено)

1. Откройте GitHub.com и войдите
2. Нажмите кнопку **"+"** вверху справа → **"New repository"**
3. Заполните:
   - **Repository name**: `telegram-miniapp` (или любое имя)
   - **Description**: опционально
   - Выберите **Public** (или Private, если у вас Pro аккаунт)
   - **НЕ** ставьте галочки на "Add README" и других опциях
4. Нажмите **"Create repository"**

5. Откройте терминал/командную строку в папке вашего проекта:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/telegram-miniapp.git
git push -u origin main
```
(Замените `ВАШ_USERNAME` на ваш GitHub username)

### Шаг 1.5: Создание Web Service на Render

1. В панели Render нажмите **"New +"** → **"Web Service"**
2. Если ваш репозиторий уже подключен:
   - Выберите ваш репозиторий из списка
   - Нажмите **"Connect"**
3. Если репозиторий не подключен:
   - Нажмите **"Connect account"** или **"Configure account"**
   - Выберите GitHub
   - Разрешите доступ к репозиториям
   - Выберите репозиторий с вашим проектом
   - Нажмите **"Connect"**

### Шаг 1.6: Настройка Web Service

Заполните форму:

1. **Name**: `telegram-miniapp-backend` (или любое имя)
2. **Region**: выберите тот же регион, что и база данных
3. **Branch**: `main` (или `master`, если у вас master)
4. **Root Directory**: оставьте пустым
5. **Runtime**: `Node`
6. **Build Command**: 
   ```
   cd backend && npm install
   ```
7. **Start Command**: 
   ```
   cd backend && node server.js
   ```
8. **Plan**: выберите **Free**

### Шаг 1.7: Добавление переменных окружения

1. В той же форме прокрутите вниз до раздела **"Environment Variables"**
2. Нажмите **"Add Environment Variable"**
3. Добавьте переменные по одной:

   **Первая переменная:**
   - **Key**: `NODE_ENV`
   - **Value**: `production`
   - Нажмите **"Add"**

   **Вторая переменная:**
   - **Key**: `DATABASE_URL`
   - **Value**: вставьте URL базы данных, который вы скопировали в Шаге 1.3
   - Нажмите **"Add"**

   **Третья переменная:**
   - **Key**: `PORT`
   - **Value**: `3000`
   - Нажмите **"Add"**

4. Проверьте, что все три переменные добавлены

### Шаг 1.8: Создание и получение URL

1. Прокрутите вниз и нажмите **"Create Web Service"**
2. ⏳ Подождите 3-5 минут, пока Render собирает и деплоит ваш проект
3. Следите за логами в разделе **"Logs"** - там не должно быть ошибок
4. После успешного деплоя вы увидите сообщение **"Your service is live"**
5. Скопируйте **URL** вашего сервиса (выглядит так: `https://telegram-miniapp-backend.onrender.com`)
6. ⚠️ **ВАЖНО**: Сохраните этот URL - он понадобится для frontend!

---

## 🎨 ЧАСТЬ 2: ДЕПЛОЙ FRONTEND НА NETLIFY

### Шаг 2.1: Регистрация на Netlify

1. Откройте сайт: https://netlify.com
2. Нажмите кнопку **"Sign up"** или **"Log in"**
3. Выберите **"Sign up with GitHub"** (рекомендуется)
4. Разрешите доступ к вашим репозиториям

### Шаг 2.2: Создание нового сайта

1. В панели Netlify нажмите кнопку **"Add new site"** → **"Import an existing project"**
2. Выберите **"Deploy with GitHub"**
3. Если нужно, авторизуйтесь через GitHub
4. Выберите репозиторий с вашим проектом из списка
5. Нажмите на репозиторий

### Шаг 2.3: Настройка сборки

Заполните форму:

1. **Branch to deploy**: `main` (или `master`)
2. **Base directory**: оставьте пустым
3. **Build command**: 
   ```
   cd frontend && npm install && npm run build
   ```
4. **Publish directory**: 
   ```
   frontend/dist
   ```

### Шаг 2.4: Добавление переменной окружения

1. Прокрутите вниз до раздела **"Environment variables"**
2. Нажмите **"New variable"**
3. Добавьте переменную:
   - **Key**: `VITE_API_URL`
   - **Value**: вставьте URL вашего backend с Render (который вы сохранили в Шаге 1.8)
   - Пример: `https://telegram-miniapp-backend.onrender.com`
4. Нажмите **"Add variable"**

### Шаг 2.5: Деплой

1. Прокрутите вниз и нажмите кнопку **"Deploy site"**
2. ⏳ Подождите 2-3 минуты, пока Netlify собирает проект
3. Следите за процессом в разделе **"Deploy log"**
4. После успешного деплоя вы увидите **"Site is live"**
5. Скопируйте **URL** вашего сайта (выглядит так: `https://random-name-12345.netlify.app`)
6. ⚠️ **ВАЖНО**: Сохраните этот URL - он понадобится для настройки Telegram бота!

### Шаг 2.6: (Опционально) Изменение имени сайта

1. В настройках сайта нажмите **"Site settings"**
2. Прокрутите до **"Change site name"**
3. Введите желаемое имя (например: `my-telegram-game`)
4. Ваш новый URL будет: `https://my-telegram-game.netlify.app`

---

## 🤖 ЧАСТЬ 3: НАСТРОЙКА TELEGRAM БОТА

### Шаг 3.1: Открытие BotFather

1. Откройте Telegram на телефоне или компьютере
2. Найдите бота **@BotFather** в поиске
3. Нажмите **"Start"** или отправьте команду `/start`

### Шаг 3.2: Настройка Menu Button (кнопки меню)

1. Отправьте команду `/mybots`
2. Выберите вашего бота из списка (или создайте нового через `/newbot`)
3. Нажмите **"Bot Settings"** или отправьте `/setmenubutton`
4. Выберите вашего бота
5. Нажмите **"Configure menu button"** или отправьте `/setmenubutton`
6. Выберите вашего бота снова
7. Введите текст кнопки: `🎮 Играть` (или любой другой)
8. Нажмите **"Web App"** или отправьте `web_app`
9. Введите URL вашего Netlify сайта (который вы сохранили в Шаге 2.5)
   - Пример: `https://my-telegram-game.netlify.app`
10. Готово! Кнопка меню настроена

### Шаг 3.3: Установка команд бота (опционально)

1. Отправьте команду `/setcommands`
2. Выберите вашего бота
3. Отправьте следующий текст:
```
start - Запустить игру
play - Играть
```
4. Готово!

### Шаг 3.4: Проверка работы

1. Откройте вашего бота в Telegram
2. Вы должны увидеть кнопку **"🎮 Играть"** внизу экрана (или в меню)
3. Нажмите на неё
4. Должно открыться ваше приложение!
5. Попробуйте кликнуть по монете - должны зарабатываться монеты
6. Проверьте лидерборд

---

## ✅ ПРОВЕРКА ЧТО ВСЁ РАБОТАЕТ

### Проверка Backend:

1. Откройте URL вашего backend в браузере
2. Добавьте `/health` в конец URL
3. Пример: `https://telegram-miniapp-backend.onrender.com/health`
4. Должен вернуться ответ: `{"status":"ok"}`

### Проверка Frontend:

1. Откройте URL вашего Netlify сайта в браузере
2. Должна открыться страница с игрой
3. Если видите ошибку подключения к API - проверьте переменную `VITE_API_URL` в Netlify

### Проверка Telegram Bot:

1. Откройте бота в Telegram
2. Нажмите кнопку меню
3. Приложение должно открыться и работать

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Backend не запускается на Render

**Решение:**
- Проверьте логи в Render (раздел "Logs")
- Убедитесь, что `DATABASE_URL` правильный
- Проверьте, что в `package.json` указана правильная версия Node.js

### Проблема: Frontend не подключается к Backend

**Решение:**
- Проверьте переменную `VITE_API_URL` в Netlify
- Убедитесь, что URL начинается с `https://`
- Проверьте, что backend работает (откройте `/health` endpoint)

### Проблема: Telegram не открывает приложение

**Решение:**
- Убедитесь, что URL в BotFather правильный и начинается с `https://`
- Подождите 5-10 минут после настройки (Telegram кэширует настройки)
- Перезапустите бота (отправьте `/start`)

### Проблема: Данные не сохраняются

**Решение:**
- Проверьте подключение к базе данных в Render
- Убедитесь, что `DATABASE_URL` правильный
- Проверьте логи backend на наличие ошибок

---

## 📝 ЧЕКЛИСТ ПЕРЕД ЗАВЕРШЕНИЕМ

- [ ] Backend задеплоен на Render и работает (`/health` возвращает `{"status":"ok"}`)
- [ ] Frontend задеплоен на Netlify и открывается в браузере
- [ ] В Netlify добавлена переменная `VITE_API_URL` с URL backend
- [ ] В Render добавлены переменные: `NODE_ENV`, `DATABASE_URL`, `PORT`
- [ ] В BotFather настроена кнопка меню с URL Netlify сайта
- [ ] Приложение открывается в Telegram и работает
- [ ] Можно кликать и зарабатывать монеты
- [ ] Лидерборд отображается

---

## 🎉 ГОТОВО!

Если все пункты чеклиста выполнены - ваше приложение полностью работает!

**Ваши URL:**
- Backend: `https://ваш-backend.onrender.com`
- Frontend: `https://ваш-сайт.netlify.app`

Сохраните эти URL для будущего использования.

---

## 💡 ДОПОЛНИТЕЛЬНЫЕ СОВЕТЫ

1. **Бесплатные планы Render** могут "засыпать" после 15 минут неактивности. Первый запрос может занять 30-60 секунд для "пробуждения". Это нормально для бесплатного плана.

2. **Netlify** также может иметь небольшие задержки на бесплатном плане, но обычно работает быстрее.

3. Если нужно, можно обновить код - просто сделайте `git push` в GitHub, и Render/Netlify автоматически пересоберут проект.

4. Для продакшена рекомендуется использовать платные планы для лучшей производительности.

---

**Удачи с вашим приложением! 🚀**

